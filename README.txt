RUN THE app_gui.py :

streamlit run app_gui.py

IN THE INTERFACE:

add the llm api_keys 

set the max websites to scrape
set the async threads 

add the input excel  **( make sure to add company website in the excel)

click the run button.